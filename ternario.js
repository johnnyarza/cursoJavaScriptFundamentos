const resultado = nota => nota >= 7 ? 'Aprovado' : 'Reprovado' // operadaor ternario = 3 termos. (comparação) ? (valor se verdadeiro) : (valor se falso) 
console.log(resultado(6.5))
console.log(resultado(8))
